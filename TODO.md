---
name: todo
description: "{{YOUR_NAME}}'s to-do list — one list for everything intermittent (recurring maintenance, one-shot reminders, follow-ups); surfaced per conventions/todo-list.md — daily digest, \"todos\" opens it any time"
type: project
last-surfaced: never
---

# TODO — {{YOUR_NAME}}'s to-do list

One list for everything intermittent: recurring chores, one-shot reminders, follow-ups.
Mechanism: [[todo-list]] — after answering, at most once a day, Claude surfaces a compact
digest of everything due; saying **"todos"** opens this list any time.

Field cheat-sheet: `when:` = freeform source (date / cadence / condition) · `next:` =
Claude-maintained date cache (`—` + parked note = never surfaces) · `needs:` = what "go"
requires · `hard: yes → bridged:` = backed by a real alarm.

## Active

### Example: monthly brain consolidation *(EXAMPLE — replace with your real items)*
- **what:** run the consolidation prompt from `docs/maintenance.md` — merge duplicate notes, flag stale facts, fix INDEX drift.
- **when:** ~monthly
- **next:** — (parked until you adopt it — say "add …" or "remind me to …" to create real items)
- **last:** never · **priority:** low
- **needs:** any machine with the brain clone

## Muted

*(nothing muted yet — items land here with date + reason, so judgment knows not to re-raise them)*

## Done log

*(newest first, one line each; recurring items also update their `last:`)*
