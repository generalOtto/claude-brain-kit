{
  "name": "brain-voice",
  "version": "1.0.0",
  "description": "Registers your self-hosted Voice Brain MCP server (deployed from tools/voice-brain) in Claude Code. Only needed when `claude mcp list` does not already show it.",
  "author": { "name": "claude-brain-kit" },
  "homepage": "https://github.com/generalOtto/claude-brain-kit",
  "license": "MIT",
  "userConfig": {
    "voice_brain_url": {
      "type": "string",
      "title": "Voice Brain MCP URL",
      "description": "The full URL printed by tools/voice-brain/setup.sh — https://<host>/mcp/<secret>. The secret path IS the credential; it is kept out of settings.json — in the macOS Keychain, or ~/.claude/.credentials.json elsewhere.",
      "sensitive": true,
      "required": true
    }
  }
}
